MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=kapido
MONGODB_COLLECTION=predictions
# For Render, use an absolute path on a mounted disk (example): /var/data/model_prod.pkl
MODEL_PATH=models/model_prod.pkl
# Comma-separated list, no spaces. Example: https://kapido-frontend.onrender.com,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000
